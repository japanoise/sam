# top level
root = true

# utf-8, unix line endings, final nls, clean whitespace - the One True Way
[*]
end_of_line = lf
insert_final_newline = true
charset = utf-8
trim_trailing_whitespace = true

# Tabs are skub, but I like them better
[*.{c,h}]
indent_style = tab
tab_width = 8
